import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.qualiteklab.ai',
  appName: 'Qualitek Lab',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
