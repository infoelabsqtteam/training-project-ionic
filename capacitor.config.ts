import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hardgoodlbs.ai',
  appName: 'E-Labs',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
